- 构建需通过 Fastlane 注入环境变量：`BUILD_NUMBER`、`VERSION_NUMBER`、`KEYSTORE_FILE_PATH`、`KEYSTORE_FILE_PASSWORD`、`KEYSTORE_FILE_ALIAS` 及 `PLAYSTORE_CREDENTIALS`。
- 执行 `fastlane build` 进行签名打包，执行 `fastlane release` 上传至 Google Play Store。