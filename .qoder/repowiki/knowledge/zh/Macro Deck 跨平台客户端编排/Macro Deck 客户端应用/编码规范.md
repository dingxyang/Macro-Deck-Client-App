- 服务类统一使用 `@Injectable({ providedIn: 'root' })` 声明为单例，并通过构造函数进行依赖注入。
- 页面组件实现 Ionic 生命周期接口（如 `ViewDidEnter`, `ViewDidLeave`）以管理订阅和资源释放。
- 数据模型定义在 `src/app/datatypes` 目录下，使用 TypeScript `interface` 描述数据结构。
- 异步操作统一使用 `async/await` 语法，特别是在处理 Storage 和 Modal 交互时。