/** 屏幕方向类型枚举，控制设备的屏幕旋转锁定方式 */
export enum ScreenOrientationType {
  /** 自动旋转 */
  Auto = 0,
  /** 横屏（正向） */
  Landscape = 1,
  /** 横屏（反向） */
  LandscapeAlt = 2,
  /** 竖屏（正向） */
  Portrait = 3,
  /** 竖屏（反向） */
  PortraitAlt = 4
}
