@echo off
rem Copy this file to scripts\local\android-signing.cmd and fill in local secrets.

set "KEYSTORE_FILE_PASSWORD=your_keystore_password"

rem Optional overrides:
rem set "BUILD_NUMBER=3001"
rem set "VERSION_NUMBER=3.0.0"
rem set "KEYSTORE_FILE_PATH=%USERPROFILE%\keystore\macro-deck-client-keystore.jks"
rem set "KEYSTORE_FILE_ALIAS=macro-deck-client"
